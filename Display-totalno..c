//Create a program to input a sentence and display the total number of words

#include<stdio.h>
#include<string.h>
int main()
{
    char string[10];
	int i;
	printf("input a sentence");
	gets=(string);
	for(i=0;string[i]=0;i++)
	{
		printf("no. of words is=%s" & string);
	}
}
