//Write a program to calculate the length of a string using pointers. 
#include<stdio.h>
void main()
char len[50];
char *pt;
{
printf("Enter a string");
fgets(str,sizeof(str)stdin);
pt=str;
while=ptr!='\0';
{
	length++
	pt++
}
if(length>0&&str[len=1]=='\n')
length--;
printf("The length of the string=%d\n", len);
}
