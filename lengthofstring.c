#include<stdio.h>
#include<string.h>
	void main ()
{
	int len,i,count,s;
	char length[20];
	printf("enter the length");
	gets(length);
	scanf("%s", length);
	for(i=0;s[i]!=0;i++)
	count++;
	printf("len=%s \n", length);
}
